# Claude Code worktrees (temp session copies)
.claude/worktrees/

# AI assistant config — keep locally, not needed in repo
codex/

# Dev/test artifacts
screenshots/
ss_*.png

# Node tooling — Playwright, used locally for visual verification only
node_modules/
package.json
package-lock.json

# ...except the Nuxt migration project (nuxt-migration branch), which is a
# real Node project—its package.json/lockfile must be tracked for reproducible
# builds in GitHub Actions.
!nuxt-app/package.json
!nuxt-app/package-lock.json
