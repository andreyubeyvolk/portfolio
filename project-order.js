// Canonical, hand-maintained order of REAL (published) project pages per
// section. Placeholder cards (href="#" on the listing pages) aren't listed
// here—"Next project" only ever chains between projects that actually
// exist. When a new project ships, add its folder slug to the matching
// array, in the order it should appear in the Next-project sequence.
window.PROJECT_ORDER = {
  inhouse: ["igaming", "crypto", "finance", "identity", "greenflag"],
  brands: [],
};
